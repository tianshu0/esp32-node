# oled_display：SSD1315 0.96" 128x64 单色 OLED（LVGL 9 + esp_lvgl_port 单色 I1）
idf_component_register(
    SRCS
        "OledDisplay.cpp"
    INCLUDE_DIRS
        "include"
    REQUIRES
        app_config
        sensor_registry
        ble_peripheral
        i2c_bus
        esp_lcd
        esp_driver_i2c
        esp_common
        log
        lvgl
    PRIV_REQUIRES
        esp_lvgl_port
)
